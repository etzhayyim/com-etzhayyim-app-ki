{
  // NOTE: this app used to be built by SvelteKit's @sveltejs/adapter-cloudflare,
  // which emitted both a server-rendering Worker script (`main`, the deployed
  // XRPC handler at svelte/src/routes/xrpc/[...path]/+server.ts) and a static
  // client bundle (`assets.directory`, the +page.svelte status page). The
  // frontend was migrated to a plain ClojureScript (shadow-cljs + reagent +
  // re-frame + jp-go-dds) static bundle with no server-rendering step — see
  // cljs/ and the repo README. `main` is dropped (a Cloudflare Worker can be
  // assets-only) and `assets.directory` now points at the cljs build output.
  //
  // `main` is NOT repointed at the moved `src/xrpc-dispatcher.ts`: it does not
  // call `env.ASSETS.fetch`, so putting it in front of the assets would mean
  // nothing serves the static files. It remains orphaned source (it also
  // still imports from '@sveltejs/kit', which no longer resolves once the
  // SvelteKit toolchain (`svelte/`) is removed) — see that file's header.
  //
  // This edit is UNVERIFIED: `wrangler deploy`/`wrangler dev` were not run
  // against it.
  "name": "kotodama-k1k1b001",
  "compatibility_date": "2025-03-17",
  "compatibility_flags": [
    "nodejs_compat",
    "nodejs_als"
  ],
  "vars": {
    "APP_CAPABILITIES": "[\"vascular-synthesis\",\"knowledge-artifact\",\"growth-ring\",\"llm-synthesis\"]",
    "APP_DESCRIPTION": "ki (木) — vascular synthesis layer. Absorbs raw signals via xylem, synthesizes structured knowledge artifacts via LLM, publishes via phloem bloom, and records growth ring checkpoints.",
    "APP_DISPLAY_NAME": "Ki Vascular Synthesis",
    "APP_FRAMEWORK": "cljs-reagent-re-frame-jp-go-dds",
    "APP_NANOID": "k1k1b001",
    "APP_PERFORMER_TYPE": "service",
    "APP_UI_TYPE": "yoro",
    "AGENTGATEWAY_MCP_ROUTER_URL": "https://mcp.etzhayyim.com/xrpc/com.etzhayyim.mcp.message"
  },
  "rules": [
    {
      "type": "CompiledWasm",
      "globs": [
        "**/*.wasm"
      ]
    }
  ],
  "routes": [
    {
      "pattern": "k1k1b001.etzhayyim.com/*",
      "zone_name": "etzhayyim.com"
    },
    {
      "pattern": "ki.etzhayyim.com/*",
      "zone_name": "etzhayyim.com"
    }
  ],
  "assets": {
    "directory": "./cljs/public",
    "binding": "ASSETS",
    "html_handling": "auto-trailing-slash",
    "not_found_handling": "none"
  }
}
